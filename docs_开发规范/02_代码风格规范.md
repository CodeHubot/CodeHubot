# 代码风格规范

> 本文档定义了 CodeHubot 项目的代码风格规范，包括 Python/FastAPI 和 Vue 3/JavaScript 的编码规范。

## 📋 目录

- [Python/FastAPI 编码规范](#pythonfastapi-编码规范)
- [Vue 3/JavaScript 编码规范](#vue-3javascript-编码规范)
- [通用规范](#通用规范)
- [代码审查检查清单](#代码审查检查清单)

---

## 🐍 Python/FastAPI 编码规范

### 1. 基本原则

遵循 [PEP 8](https://www.python.org/dev/peps/pep-0008/) Python 代码风格指南。

### 2. 命名约定

```python
# ✅ 正确的命名方式

# 模块名和包名: 小写字母，下划线分隔
# models/course_model.py
# utils/device_helper.py

# 类名: PascalCase（大驼峰）
class CourseTemplate:
    pass

class Agent:
    pass

# 函数名和变量名: snake_case（小写下划线）
def get_course_list():
    pass

def create_new_student():
    pass

user_id = 123
course_name = "Python 基础"

# 常量: UPPER_CASE（全大写下划线）
MAX_PAGE_SIZE = 100
DEFAULT_TIMEOUT = 30
API_VERSION = "v1"

# 私有变量和方法: 前缀单下划线
def _internal_helper():
    pass

_cache_data = {}

# 强私有: 前缀双下划线（避免子类覆盖）
def __private_method():
    pass
```

#### ❌ 错误示例

```python
# ❌ 不要使用驼峰命名函数
def getCourseList():  # 错误
    pass

# ❌ 不要使用下划线命名类
class course_template:  # 错误
    pass

# ❌ 常量不要用小写
max_page_size = 100  # 错误
```

### 3. 导入顺序

```python
"""
模块文档字符串
说明模块的作用
"""

# 1. 标准库导入
import os
import sys
from datetime import datetime
from typing import Optional, List

# 2. 第三方库导入
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.orm import Session
from pydantic import BaseModel

# 3. 本地应用/库导入
from app.core.deps import get_db, get_current_user
from app.core.response import success_response
from app.models.course import Course
from app.schemas.course import CourseCreate

# 每组导入之间空一行
```

### 4. 类型注解

**必须为函数参数和返回值添加类型注解：**

```python
from typing import Optional, List, Dict
from sqlalchemy.orm import Session

# ✅ 正确：完整的类型注解
def get_course_by_id(
    course_id: int,
    db: Session,
    include_deleted: bool = False
) -> Optional[Course]:
    """获取课程详情"""
    query = db.query(Course).filter(Course.id == course_id)
    if not include_deleted:
        query = query.filter(Course.is_deleted == False)
    return query.first()

def get_courses_list(
    page: int = 1,
    page_size: int = 20,
    db: Session = None
) -> List[Dict]:
    """获取课程列表"""
    courses = db.query(Course).offset((page - 1) * page_size).limit(page_size).all()
    return [course.to_dict() for course in courses]

# ❌ 错误：缺少类型注解
def get_course_by_id(course_id, db):  # 错误
    pass
```

### 5. 文档字符串

使用 **Google 风格** 的文档字符串：

```python
def create_course(
    course_data: CourseCreate,
    db: Session,
    current_user: User
) -> Course:
    """创建新课程
    
    Args:
        course_data: 课程创建数据
        db: 数据库会话
        current_user: 当前用户
        
    Returns:
        创建的课程对象
        
    Raises:
        HTTPException: 当课程名称重复时
        
    Example:
        >>> course_data = CourseCreate(title="Python 基础", description="适合初学者")
        >>> course = create_course(course_data, db, user)
    """
    # 检查课程名称是否重复
    existing_course = db.query(Course).filter(
        Course.title == course_data.title,
        Course.school_id == current_user.school_id
    ).first()
    
    if existing_course:
        raise HTTPException(status_code=400, detail="课程名称已存在")
    
    # 创建课程
    course = Course(**course_data.dict(), created_by=current_user.id)
    db.add(course)
    db.commit()
    db.refresh(course)
    
    return course
```

### 6. 代码格式

```python
# 每行最大长度: 120 字符（可适当放宽到 120）
# 缩进: 4 个空格（不使用 Tab）

# ✅ 正确的格式

# 函数定义
def function_with_many_parameters(
    param1: str,
    param2: int,
    param3: Optional[str] = None,
    param4: bool = False
) -> Dict:
    pass

# 列表、字典换行
course_data = {
    "title": "Python 基础",
    "description": "适合初学者",
    "duration": 30,
    "status": "draft"
}

# 长条件判断
if (
    course.status == "published"
    and course.school_id == current_user.school_id
    and not course.is_deleted
):
    pass

# 链式调用
courses = (
    db.query(Course)
    .filter(Course.school_id == school_id)
    .filter(Course.is_deleted == False)
    .order_by(Course.created_at.desc())
    .all()
)
```

### 7. 异常处理

```python
from fastapi import HTTPException
import logging

logger = logging.getLogger(__name__)

# ✅ 正确的异常处理
@router.post("/courses")
def create_course(course: CourseCreate, db: Session = Depends(get_db)):
    try:
        # 业务逻辑
        new_course = Course(**course.dict())
        db.add(new_course)
        db.commit()
        db.refresh(new_course)
        
        return success_response(data=new_course.to_dict())
        
    except IntegrityError as e:
        # 特定异常处理
        db.rollback()
        logger.warning(f"课程创建违反约束: {str(e)}")
        raise HTTPException(status_code=400, detail="课程名称已存在")
        
    except Exception as e:
        # 通用异常处理
        db.rollback()
        logger.error(f"创建课程失败: {str(e)}", exc_info=True)
        raise HTTPException(status_code=500, detail="创建失败")

# ❌ 错误：捕获所有异常但不处理
try:
    # 危险操作
    pass
except:  # 不要使用裸 except
    pass
```

### 8. 代码组织

```python
"""
课程管理 API
提供课程的增删改查功能
"""
from typing import Optional, List
from fastapi import APIRouter, Depends, HTTPException, Query
from sqlalchemy.orm import Session

from app.core.deps import get_db, get_current_admin
from app.core.response import success_response
from app.models.agent import Agent
from app.schemas.agent import AgentCreate, AgentUpdate

# 创建路由
router = APIRouter()


# 常量定义
MAX_PAGE_SIZE = 100
DEFAULT_PAGE_SIZE = 20


# 辅助函数（私有）
def _check_agent_permission(agent_id: int, user_id: int, db: Session) -> Agent:
    """检查智能体权限"""
    agent = db.query(Agent).filter(Agent.id == agent_id).first()
    if not course:
        raise HTTPException(status_code=404, detail="课程不存在")
    if course.created_by != user_id:
        raise HTTPException(status_code=403, detail="无权访问")
    return course


# API 路由（公开）
@router.get("/courses")
def list_courses(
    page: int = Query(1, ge=1),
    page_size: int = Query(DEFAULT_PAGE_SIZE, ge=1, le=MAX_PAGE_SIZE),
    db: Session = Depends(get_db)
):
    """获取课程列表"""
    pass


@router.post("/courses")
def create_course(
    course: CourseCreate,
    db: Session = Depends(get_db)
):
    """创建课程"""
    pass
```

### 9. 避免魔法数字

```python
# ❌ 错误：使用魔法数字
if user.role == 1:  # 1 是什么？
    pass

if course.status == "active":  # 状态值硬编码
    pass

# ✅ 正确：使用常量或枚举
from enum import Enum

class UserRole(str, Enum):
    ADMIN = "admin"
    TEACHER = "teacher"
    STUDENT = "student"

class CourseStatus(str, Enum):
    DRAFT = "draft"
    PUBLISHED = "published"
    ARCHIVED = "archived"

# 使用
if user.role == UserRole.ADMIN:
    pass

if course.status == CourseStatus.PUBLISHED:
    pass
```

### 10. 列表推导式和生成器

```python
# ✅ 简单清晰的列表推导
course_ids = [course.id for course in courses]
active_courses = [c for c in courses if c.status == "active"]

# ✅ 复杂逻辑使用普通循环
result = []
for course in courses:
    if course.status == "active":
        # 复杂处理逻辑
        processed_data = process_course(course)
        if processed_data:
            result.append(processed_data)

# ✅ 大数据使用生成器
course_names = (course.title for course in courses)  # 不会一次性加载所有数据
```

---

## 🎨 Vue 3/JavaScript 编码规范

### 1. 基本原则

遵循 [Vue 3 风格指南](https://cn.vuejs.org/style-guide/) 和 JavaScript Standard Style。

### 2. 命名约定

```javascript
// ✅ 正确的命名方式

// 组件文件名: PascalCase（大驼峰）
// CourseList.vue
// StudentDetail.vue
// AdminDashboard.vue

// 组件名: PascalCase
export default {
  name: 'CourseList'
}

// 变量和函数: camelCase（小驼峰）
const courseList = []
const userId = 123

function getCourseDetail() {}
function handleSubmit() {}

// 常量: UPPER_CASE（全大写下划线）
const MAX_PAGE_SIZE = 100
const API_BASE_URL = '/api/v1'
const DEFAULT_TIMEOUT = 5000

// 私有变量/函数: 前缀下划线（约定）
const _internalCache = {}
function _formatData() {}

// Boolean 变量: is/has/should 前缀
const isActive = true
const hasPermission = false
const shouldUpdate = true
```

### 3. 组件结构

```vue
<template>
  <!-- 模板必须有一个根元素（Vue 3 可以多个，但建议单个） -->
  <div class="course-list">
    <el-card>
      <template #header>
        <div class="card-header">
          <span>课程列表</span>
          <el-button type="primary" @click="handleCreate">创建课程</el-button>
        </div>
      </template>
      
      <!-- 内容 -->
      <el-table :data="courses" :loading="loading">
        <el-table-column prop="title" label="标题" />
        <el-table-column prop="status" label="状态" />
      </el-table>
    </el-card>
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'
import { ElMessage } from 'element-plus'
import { getAgents, createAgent } from '@/modules/ai/api/agents'

// ===== 响应式数据 =====
const loading = ref(false)
const courses = ref([])
const total = ref(0)

// ===== 生命周期 =====
onMounted(() => {
  loadCourses()
})

// ===== 方法 =====
async function loadCourses() {
  try {
    loading.value = true
    const data = await getCourses({ page: 1, pageSize: 20 })
    courses.value = data.items
    total.value = data.total
  } catch (error) {
    console.error('加载失败:', error)
  } finally {
    loading.value = false
  }
}

function handleCreate() {
  // 创建逻辑
}
</script>

<style scoped>
/* 使用 scoped 避免样式污染 */
.course-list {
  padding: 20px;
}

.card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
}
</style>
```

### 4. Composition API 规范

```vue
<script setup>
import { ref, computed, watch, onMounted } from 'vue'

// ===== Props 定义 =====
const props = defineProps({
  courseId: {
    type: String,
    required: true
  },
  readonly: {
    type: Boolean,
    default: false
  }
})

// ===== Emits 定义 =====
const emit = defineEmits(['update', 'delete'])

// ===== 响应式数据 =====
const course = ref(null)
const loading = ref(false)
const formData = ref({
  title: '',
  description: ''
})

// ===== 计算属性 =====
const isPublished = computed(() => {
  return course.value?.status === 'published'
})

const canEdit = computed(() => {
  return !props.readonly && !isPublished.value
})

// ===== 监听器 =====
watch(
  () => props.courseId,
  (newId) => {
    if (newId) {
      loadCourse(newId)
    }
  },
  { immediate: true }
)

// ===== 生命周期 =====
onMounted(() => {
  console.log('组件已挂载')
})

// ===== 方法 =====
async function loadCourse(id) {
  try {
    loading.value = true
    const data = await getCourseDetail(id)
    course.value = data
  } catch (error) {
    console.error('加载失败:', error)
  } finally {
    loading.value = false
  }
}

function handleUpdate() {
  emit('update', formData.value)
}

// ===== 暴露给父组件的方法 =====
defineExpose({
  loadCourse,
  handleUpdate
})
</script>
```

### 5. API 调用规范

```javascript
/**
 * 课程管理 API
 * @module api/courses
 */
import request from './request'

/**
 * 获取课程列表
 * @param {Object} params - 查询参数
 * @param {number} params.page - 页码
 * @param {number} params.pageSize - 每页数量
 * @param {string} [params.search] - 搜索关键词
 * @returns {Promise<Object>} 课程列表数据
 */
export function getCourses(params) {
  return request({
    url: '/api/courses',
    method: 'get',
    params
  })
}

/**
 * 获取课程详情
 * @param {string} courseUuid - 课程 UUID
 * @returns {Promise<Object>} 课程详情
 */
export function getCourseDetail(courseUuid) {
  return request({
    url: `/api/courses/${courseUuid}`,
    method: 'get'
  })
}

/**
 * 创建课程
 * @param {Object} data - 课程数据
 * @returns {Promise<Object>} 创建的课程
 */
export function createCourse(data) {
  return request({
    url: '/api/courses',
    method: 'post',
    data
  })
}
```

### 6. 事件处理

```vue
<template>
  <div>
    <!-- ✅ 使用 handle 前缀命名事件处理函数 -->
    <el-button @click="handleSubmit">提交</el-button>
    <el-button @click="handleCancel">取消</el-button>
    
    <!-- ✅ 传递参数 -->
    <el-button @click="handleDelete(item.id)">删除</el-button>
    
    <!-- ✅ 使用事件修饰符 -->
    <form @submit.prevent="handleSubmit">
      <input @keyup.enter="handleSearch" />
    </form>
    
    <!-- ❌ 避免在模板中写复杂逻辑 -->
    <el-button @click="items.filter(i => i.status === 'active').map(i => i.id)">
      错误示例
    </el-button>
  </div>
</template>

<script setup>
function handleSubmit() {
  // 提交逻辑
}

function handleCancel() {
  // 取消逻辑
}

function handleDelete(id) {
  // 删除逻辑
}

function handleSearch() {
  // 搜索逻辑
}
</script>
```

### 7. 条件渲染和列表渲染

```vue
<template>
  <div>
    <!-- ✅ v-if 用于条件渲染 -->
    <div v-if="isLoading">加载中...</div>
    <div v-else-if="error">加载失败</div>
    <div v-else>
      <!-- ✅ v-for 必须带 key -->
      <div
        v-for="course in courses"
        :key="course.id"
        class="course-item"
      >
        {{ course.title }}
      </div>
    </div>
    
    <!-- ✅ v-show 用于频繁切换的显示/隐藏 -->
    <div v-show="showDetail">详情内容</div>
    
    <!-- ❌ 避免 v-if 和 v-for 同时使用 -->
    <!-- 错误示例 -->
    <div v-for="item in items" v-if="item.active" :key="item.id">
      {{ item.name }}
    </div>
    
    <!-- ✅ 正确：使用计算属性过滤 -->
    <div v-for="item in activeItems" :key="item.id">
      {{ item.name }}
    </div>
  </div>
</template>

<script setup>
import { computed } from 'vue'

const items = ref([])

const activeItems = computed(() => {
  return items.value.filter(item => item.active)
})
</script>
```

### 8. 表单验证

```vue
<template>
  <el-form
    ref="formRef"
    :model="formData"
    :rules="formRules"
    label-width="100px"
  >
    <el-form-item label="课程名称" prop="title">
      <el-input v-model="formData.title" placeholder="请输入课程名称" />
    </el-form-item>
    
    <el-form-item label="课程描述" prop="description">
      <el-input
        v-model="formData.description"
        type="textarea"
        :rows="4"
      />
    </el-form-item>
    
    <el-form-item>
      <el-button type="primary" @click="handleSubmit">提交</el-button>
      <el-button @click="handleReset">重置</el-button>
    </el-form-item>
  </el-form>
</template>

<script setup>
import { ref, reactive } from 'vue'
import { ElMessage } from 'element-plus'

const formRef = ref(null)

const formData = reactive({
  title: '',
  description: ''
})

const formRules = {
  title: [
    { required: true, message: '请输入课程名称', trigger: 'blur' },
    { min: 2, max: 100, message: '长度在 2 到 100 个字符', trigger: 'blur' }
  ],
  description: [
    { required: false },
    { max: 500, message: '最多 500 个字符', trigger: 'blur' }
  ]
}

async function handleSubmit() {
  try {
    // 验证表单
    await formRef.value.validate()
    
    // 提交数据
    await createCourse(formData)
    ElMessage.success('创建成功')
    
  } catch (error) {
    if (error.errors) {
      // 表单验证失败
      ElMessage.warning('请检查表单')
    } else {
      // API 调用失败
      console.error('提交失败:', error)
    }
  }
}

function handleReset() {
  formRef.value.resetFields()
}
</script>
```

### 9. 代码格式

```javascript
// 使用 2 空格缩进
// 字符串使用单引号
// 对象、数组末尾添加逗号

// ✅ 正确格式
const config = {
  baseURL: '/api',
  timeout: 5000,
  headers: {
    'Content-Type': 'application/json'
  }
}

const colors = [
  'red',
  'green',
  'blue'
]

// 函数参数换行
function createCourse(
  title,
  description,
  duration,
  status
) {
  // 函数体
}

// 链式调用
const result = data
  .filter(item => item.active)
  .map(item => item.name)
  .join(', ')
```

---

## 🌐 通用规范

### 1. 注释规范

```python
# Python

# 单行注释：# 后面加一个空格
# 这是一个单行注释

def get_course(course_id: int) -> Course:
    """
    获取课程详情
    
    这是多行文档字符串，说明函数的用途
    """
    # TODO: 添加缓存机制
    # FIXME: 处理课程不存在的情况
    # NOTE: 这里需要检查权限
    pass
```

```javascript
// JavaScript

// 单行注释：// 后面加一个空格
// 这是一个单行注释

/**
 * 获取课程详情
 * 
 * 这是 JSDoc 注释，说明函数的用途
 * @param {string} courseId - 课程 ID
 * @returns {Promise<Object>} 课程数据
 */
function getCourseDetail(courseId) {
  // TODO: 添加缓存
  // FIXME: 处理错误情况
  // NOTE: 需要权限检查
}
```

### 2. 避免不必要的注释

```python
# ❌ 不好的注释：说明显而易见的事情
# 将 x 加 1
x = x + 1

# 循环遍历课程列表
for course in courses:
    pass

# ✅ 好的注释：解释为什么这样做
# 使用缓存避免重复查询数据库
if cache.has(course_id):
    return cache.get(course_id)

# 这里需要延迟 100ms 等待 DOM 更新
await sleep(0.1)
```

### 3. 代码分块

```python
def complex_function():
    """复杂函数示例"""
    
    # 1. 数据验证
    if not validate_input(data):
        raise ValueError("Invalid input")
    
    # 2. 数据处理
    processed_data = process_data(data)
    cleaned_data = clean_data(processed_data)
    
    # 3. 保存到数据库
    db.add(cleaned_data)
    db.commit()
    
    # 4. 发送通知
    send_notification(user_id, "Data processed")
    
    return cleaned_data
```

---

## ✅ 代码审查检查清单

### Python 代码审查

- [ ] 遵循 PEP 8 风格
- [ ] 函数有完整的类型注解
- [ ] 函数有文档字符串（docstring）
- [ ] 导入顺序正确（标准库 -> 第三方 -> 本地）
- [ ] 没有使用 `except:` 捕获所有异常
- [ ] 没有魔法数字，使用常量或枚举
- [ ] 变量命名清晰，见名知意
- [ ] 函数长度合理（建议 < 50 行）
- [ ] API 使用 `success_response` 返回数据
- [ ] 数据库操作有异常处理和回滚

### JavaScript/Vue 代码审查

- [ ] 组件使用 `<script setup>` 语法
- [ ] Props 和 Emits 有完整定义
- [ ] 组件命名使用 PascalCase
- [ ] 函数命名使用 camelCase
- [ ] v-for 必须带 key
- [ ] 避免 v-if 和 v-for 同时使用
- [ ] 样式使用 scoped
- [ ] API 调用有错误处理
- [ ] 没有 console.log 调试代码
- [ ] 表单有验证规则

### 通用检查

- [ ] 代码逻辑清晰，易于理解
- [ ] 没有重复代码（DRY 原则）
- [ ] 注释清晰，解释了"为什么"而不是"是什么"
- [ ] 没有硬编码的敏感信息（密码、密钥等）
- [ ] 变量命名有意义，不使用 a, b, c, temp 等
- [ ] 函数职责单一（Single Responsibility）
- [ ] 代码可测试

---

## 🛠️ 推荐工具

### Python

- **black**: 自动格式化代码
- **flake8**: 代码风格检查
- **mypy**: 类型检查
- **pylint**: 代码质量检查

### JavaScript/Vue

- **ESLint**: 代码风格和质量检查
- **Prettier**: 自动格式化代码
- **Vetur** / **Volar**: Vue 开发工具（VS Code）

---

**记住**: 统一的代码风格让团队协作更顺畅，代码更易维护！
