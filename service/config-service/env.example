# ==========================================
# CodeHubot 配置服务环境变量配置
# 使用前请复制此文件为 .env 并修改配置
# ==========================================

# ==================== 数据库配置 ====================
DB_HOST=localhost
DB_PORT=3306
DB_USER=aiot_user
DB_PASSWORD=your_secure_password
DB_NAME=aiot_admin

# ==================== MQTT配置（设备连接配置）====================
# 注意：这是告诉物联网设备的外部MQTT Broker地址
# 需要填写可以从外部访问的域名或IP地址
# 示例：mqtt.example.com 或 192.168.1.100
DEVICE_MQTT_BROKER=your-mqtt-broker-address
DEVICE_MQTT_PORT=1883
DEVICE_MQTT_USE_SSL=false

# ==================== API服务器配置 ====================
# 主API服务器地址（后端服务地址）
API_SERVER=http://localhost:8000

# OTA固件服务器地址（通常与API_SERVER相同）
OTA_SERVER=http://localhost:8000

# ==================== 服务配置 ====================
# 服务监听端口
PORT=8001

# ==================== 速率限制配置 ====================
# 每个IP在时间窗口内的最大请求数
RATE_LIMIT_REQUESTS=10

# 时间窗口（秒）
RATE_LIMIT_WINDOW=60

# ==================== 日志配置 ====================
# 日志级别：DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
