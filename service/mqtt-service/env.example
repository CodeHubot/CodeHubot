# MQTT服务配置

# ==========================================
# MQTT Broker 配置（MQTT服务连接到Broker）
# ==========================================
# 注意：这是 mqtt-service 连接到 MQTT Broker 的地址
# Docker 环境：使用容器名 "mosquitto"
# 本地开发：使用 "localhost"
MQTT_SERVICE_BROKER=mosquitto
MQTT_SERVICE_PORT=1883

# MQTT 认证（如果 Broker 需要认证）
MQTT_SERVICE_USERNAME=
MQTT_SERVICE_PASSWORD=

# ==========================================
# 数据库配置
# ==========================================
DB_HOST=localhost
DB_PORT=3306
DB_NAME=aiot_admin
DB_USER=root
DB_PASSWORD=your_password

# ==========================================
# 日志配置（可选）
# ==========================================
# LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR
# LOG_FILE=mqtt_service.log


