# ==========================================
# CodeHubot 插件后端服务环境变量配置
# 使用前请复制此文件为 .env 并修改配置
# ==========================================

# ==================== 服务配置 ====================
# 端口 9002（9001 被 MQTT WebSocket 占用）
SERVICE_PORT=9002
SERVICE_HOST=0.0.0.0

# ==================== 数据库配置 ====================
DB_HOST=localhost
DB_PORT=3306
DB_NAME=aiot_admin
DB_USER=aiot_user
DB_PASSWORD=your_secure_password

# ==================== MQTT配置 ====================
# MQTT服务器地址（本地开发：localhost，Docker环境：mqtt）
MQTT_BROKER_HOST=localhost
MQTT_BROKER_PORT=1883

# MQTT认证（可选，Mosquitto默认无需认证）
MQTT_USERNAME=
MQTT_PASSWORD=
