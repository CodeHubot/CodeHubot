# 调试技巧

## 概述

本文档介绍 CodeHubot 平台开发过程中的调试技巧和最佳实践，帮助开发者快速定位和解决问题。

## 后端调试

### 1. 日志调试

#### 配置日志级别

**文件位置**: `backend/app/core/logging_config.py`

```python
import logging
import sys

def setup_logging(log_level: str = "INFO"):
    """配置日志"""
    
    # 设置日志格式
    log_format = '%(asctime)s - %(name)s - %(levelname)s - %(message)s'
    
    # 配置根日志
    logging.basicConfig(
        level=getattr(logging, log_level.upper()),
        format=log_format,
        handlers=[
            logging.StreamHandler(sys.stdout),
            logging.FileHandler('logs/app.log')
        ]
    )
    
    # 为特定模块设置日志级别
    logging.getLogger('sqlalchemy.engine').setLevel(logging.WARNING)
    logging.getLogger('uvicorn').setLevel(logging.INFO)
```

#### 使用日志

```python
import logging

logger = logging.getLogger(__name__)

# 不同级别的日志
logger.debug("调试信息：变量值 = %s", variable)
logger.info("✅ 操作成功：用户 %s 已创建", user.username)
logger.warning("⚠️  警告：配置项缺失")
logger.error("❌ 错误：数据库连接失败", exc_info=True)
logger.critical("🚨 严重错误：系统崩溃")

# 结构化日志
logger.info("用户登录", extra={
    "user_id": user.id,
    "ip": request.client.host,
    "timestamp": datetime.utcnow()
})
```

#### 日志最佳实践

```python
# ✅ 好的日志
logger.info(f"用户 {user.email} 创建设备 {device.name} (ID: {device.id})")

# ❌ 不好的日志
logger.info("创建成功")  # 没有上下文信息

# ✅ 记录关键操作
logger.info(f"数据库查询: {query} (耗时: {elapsed}ms)")

# ✅ 记录错误时包含堆栈
try:
    risky_operation()
except Exception as e:
    logger.error("操作失败", exc_info=True)  # 自动记录堆栈
```

### 2. Python 调试器 (pdb)

#### 使用 pdb

```python
# 在需要调试的地方插入断点
import pdb; pdb.set_trace()

# 或使用 Python 3.7+ 的 breakpoint()
breakpoint()
```

#### pdb 常用命令

```bash
# 基本命令
n (next)         # 执行下一行
s (step)         # 进入函数
c (continue)     # 继续执行到下一个断点
l (list)         # 显示当前代码
p variable       # 打印变量值
pp variable      # 漂亮打印变量
w (where)        # 显示调用栈
q (quit)         # 退出调试

# 高级命令
b 10             # 在第10行设置断点
cl 1             # 清除断点1
a                # 打印当前函数的参数
!variable = 10   # 修改变量值
```

### 3. VS Code 调试

#### 配置 launch.json

```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: FastAPI",
      "type": "python",
      "request": "launch",
      "module": "uvicorn",
      "args": [
        "main:app",
        "--reload",
        "--host",
        "0.0.0.0",
        "--port",
        "8000"
      ],
      "cwd": "${workspaceFolder}/backend",
      "env": {
        "PYTHONPATH": "${workspaceFolder}/backend",
        "DEBUG": "true"
      },
      "console": "integratedTerminal",
      "justMyCode": false  // 允许调试第三方库
    },
    {
      "name": "Python: Current File",
      "type": "python",
      "request": "launch",
      "program": "${file}",
      "console": "integratedTerminal"
    }
  ]
}
```

#### 使用断点

1. 在代码行号左侧点击设置断点
2. 按 F5 启动调试
3. 使用调试控制台查看变量
4. 逐步执行代码

### 4. FastAPI 自动文档调试

#### Swagger UI

访问 http://localhost:8000/docs

**优点**：
- ✅ 可视化 API 接口
- ✅ 在线测试 API
- ✅ 查看请求/响应格式
- ✅ 自动生成示例

**使用技巧**：

```python
from fastapi import APIRouter
from pydantic import BaseModel, Field

router = APIRouter()

class UserCreate(BaseModel):
    email: str = Field(..., example="user@example.com")
    username: str = Field(..., example="john_doe")
    password: str = Field(..., min_length=6, example="password123")

@router.post("/users", tags=["用户管理"], summary="创建用户")
async def create_user(user: UserCreate):
    """
    创建新用户
    
    - **email**: 用户邮箱（必填）
    - **username**: 用户名（必填）
    - **password**: 密码（至少6位）
    """
    return {"message": "用户创建成功"}
```

#### ReDoc

访问 http://localhost:8000/redoc

更美观的 API 文档展示。

### 5. SQL 查询调试

#### 启用 SQL 日志

```python
# 在开发环境显示所有 SQL 语句
import logging
logging.getLogger('sqlalchemy.engine').setLevel(logging.INFO)
```

#### 查看生成的 SQL

```python
from sqlalchemy import select
from app.models.user import User

# 构造查询
stmt = select(User).where(User.email == "test@example.com")

# 打印 SQL
print(stmt.compile(compile_kwargs={"literal_binds": True}))
# 输出: SELECT * FROM core_users WHERE email = 'test@example.com'
```

#### 使用 EXPLAIN 分析

```python
# 获取查询执行计划
from sqlalchemy import text

result = db.execute(text("EXPLAIN SELECT * FROM core_users WHERE email = 'test@example.com'"))
for row in result:
    print(row)
```

### 6. 性能分析

#### 使用 time 模块

```python
import time

start = time.time()

# 执行操作
result = expensive_operation()

elapsed = time.time() - start
logger.info(f"操作耗时: {elapsed:.3f}秒")
```

#### 使用装饰器记录耗时

```python
import functools
import time
import logging

logger = logging.getLogger(__name__)

def timeit(func):
    """记录函数执行时间的装饰器"""
    @functools.wraps(func)
    async def async_wrapper(*args, **kwargs):
        start = time.time()
        result = await func(*args, **kwargs)
        elapsed = time.time() - start
        logger.info(f"{func.__name__} 耗时: {elapsed:.3f}秒")
        return result
    
    @functools.wraps(func)
    def sync_wrapper(*args, **kwargs):
        start = time.time()
        result = func(*args, **kwargs)
        elapsed = time.time() - start
        logger.info(f"{func.__name__} 耗时: {elapsed:.3f}秒")
        return result
    
    # 判断是否为异步函数
    if asyncio.iscoroutinefunction(func):
        return async_wrapper
    return sync_wrapper

# 使用
@timeit
async def slow_api():
    await asyncio.sleep(1)
    return {"message": "完成"}
```

#### 使用 cProfile

```python
import cProfile
import pstats

# 性能分析
profiler = cProfile.Profile()
profiler.enable()

# 执行代码
your_function()

profiler.disable()

# 输出统计
stats = pstats.Stats(profiler)
stats.sort_stats('cumulative')
stats.print_stats(20)  # 显示前20个最耗时的函数
```

## 前端调试

### 1. 浏览器开发者工具

#### Chrome DevTools

**快捷键**：
- F12 或 Cmd+Option+I (Mac) / Ctrl+Shift+I (Windows)

**常用面板**：

```
Console (控制台)
├── 查看日志输出
├── 执行 JavaScript
└── 查看错误信息

Network (网络)
├── 查看 HTTP 请求
├── 检查响应内容
├── 查看请求耗时
└── 模拟慢速网络

Sources (源代码)
├── 设置断点
├── 单步调试
├── 查看调用栈
└── 查看变量

Application (应用)
├── 查看 LocalStorage
├── 查看 Cookie
├── 查看 IndexedDB
└── 清除缓存

Vue DevTools (Vue 专用)
├── 查看组件树
├── 检查组件状态
├── 查看 Vuex/Pinia
└── 性能分析
```

### 2. Console 调试

#### 基本日志

```javascript
// 不同级别的日志
console.log('普通日志')
console.info('ℹ️ 信息')
console.warn('⚠️ 警告')
console.error('❌ 错误')

// 分组日志
console.group('用户信息')
console.log('姓名:', user.name)
console.log('邮箱:', user.email)
console.groupEnd()

// 表格显示
console.table([
  { id: 1, name: 'Alice' },
  { id: 2, name: 'Bob' }
])

// 计时
console.time('API 请求')
await fetchData()
console.timeEnd('API 请求')

// 断言
console.assert(user.age > 0, '年龄必须大于0')
```

#### 调试技巧

```javascript
// 查看对象完整结构
console.dir(complexObject)

// 查看调用栈
console.trace('函数调用路径')

// 清空控制台
console.clear()

// 计数
for (let i = 0; i < 5; i++) {
  console.count('循环次数')
}
```

### 3. Vue DevTools

#### 安装

Chrome/Edge 扩展商店搜索 "Vue.js devtools" 安装。

#### 功能

```
Components (组件)
├── 查看组件树
├── 检查 props
├── 查看 data
├── 查看 computed
└── 修改组件状态（实时生效）

Pinia (状态管理)
├── 查看 Store
├── 查看 State
├── 查看 Actions
└── 时间旅行调试

Timeline (时间线)
├── 查看事件触发
├── 查看组件生命周期
└── 性能分析

Routes (路由)
└── 查看路由信息
```

### 4. 断点调试

#### 在代码中设置断点

```javascript
function fetchData() {
  const data = getData()
  
  // 设置断点
  debugger
  
  processData(data)
}
```

#### 条件断点

在 Chrome DevTools Sources 面板：
1. 右键点击行号
2. 选择 "Add conditional breakpoint"
3. 输入条件，如 `user.id === 123`

### 5. 网络请求调试

#### 查看请求详情

```javascript
// 在请求拦截器中打印
axios.interceptors.request.use(config => {
  console.log('📤 发送请求:', config.method.toUpperCase(), config.url)
  console.log('请求头:', config.headers)
  console.log('请求体:', config.data)
  return config
})

// 响应拦截器
axios.interceptors.response.use(
  response => {
    console.log('📥 收到响应:', response.config.url)
    console.log('状态码:', response.status)
    console.log('响应数据:', response.data)
    return response
  },
  error => {
    console.error('❌ 请求失败:', error.message)
    console.error('错误详情:', error.response?.data)
    return Promise.reject(error)
  }
)
```

#### 模拟网络条件

Chrome DevTools Network 面板：
- Online: 正常网速
- Fast 3G: 快速 3G
- Slow 3G: 慢速 3G
- Offline: 离线

### 6. Vue 组件调试

#### 在模板中调试

```vue
<template>
  <div>
    <!-- 输出变量到控制台 -->
    {{ console.log('user:', user) }}
    
    <!-- 显示调试信息 -->
    <pre>{{ JSON.stringify(user, null, 2) }}</pre>
  </div>
</template>
```

#### 在脚本中调试

```vue
<script setup>
import { watch, onMounted } from 'vue'

const user = ref(null)

// 监听变化
watch(user, (newVal, oldVal) => {
  console.log('user 变化:', oldVal, '->', newVal)
}, { deep: true })

// 生命周期调试
onMounted(() => {
  console.log('组件已挂载')
  console.log('DOM 元素:', document.querySelector('.my-element'))
})
</script>
```

## 数据库调试

### 1. 查看慢查询

```sql
-- 启用慢查询日志
SET GLOBAL slow_query_log = 'ON';
SET GLOBAL long_query_time = 1;  -- 超过1秒的查询

-- 查看慢查询
SELECT * FROM mysql.slow_log ORDER BY start_time DESC LIMIT 10;
```

### 2. 分析查询性能

```sql
-- 使用 EXPLAIN
EXPLAIN SELECT * FROM core_users WHERE email = 'test@example.com';

-- 查看索引使用情况
SHOW INDEX FROM core_users;

-- 查看表状态
SHOW TABLE STATUS LIKE 'core_users';
```

### 3. 监控数据库连接

```python
from sqlalchemy import event
from sqlalchemy.engine import Engine
import logging

logger = logging.getLogger(__name__)

@event.listens_for(Engine, "before_cursor_execute")
def receive_before_cursor_execute(conn, cursor, statement, params, context, executemany):
    logger.debug(f"SQL: {statement}")
    logger.debug(f"参数: {params}")

@event.listens_for(Engine, "after_cursor_execute")
def receive_after_cursor_execute(conn, cursor, statement, params, context, executemany):
    logger.debug("查询执行完成")
```

## MQTT 调试

### 1. 使用 MQTT 客户端工具

#### MQTT Explorer

图形化工具，实时查看所有主题消息。

#### mosquitto_sub 命令

```bash
# 订阅所有主题
mosquitto_sub -h localhost -t "#" -v

# 订阅特定设备
mosquitto_sub -h localhost -t "device/ESP32_001/#" -v

# 使用认证
mosquitto_sub -h localhost -t "#" -u username -P password -v
```

### 2. 在代码中调试

```python
def on_message(client, userdata, msg):
    """MQTT 消息回调"""
    logger.debug(f"📨 收到消息")
    logger.debug(f"主题: {msg.topic}")
    logger.debug(f"Payload: {msg.payload.decode()}")
    logger.debug(f"QoS: {msg.qos}")
    logger.debug(f"Retain: {msg.retain}")
```

## 容器调试

### 1. 查看容器日志

```bash
# 查看所有日志
docker logs codehubot-backend

# 实时查看日志
docker logs -f codehubot-backend

# 查看最近100行
docker logs --tail 100 codehubot-backend

# 显示时间戳
docker logs -t codehubot-backend
```

### 2. 进入容器调试

```bash
# 进入容器 Bash
docker exec -it codehubot-backend bash

# 执行命令
docker exec codehubot-backend python -c "import sys; print(sys.version)"

# 查看环境变量
docker exec codehubot-backend env

# 查看进程
docker exec codehubot-backend ps aux
```

### 3. 容器资源监控

```bash
# 查看资源使用
docker stats

# 查看特定容器
docker stats codehubot-backend

# 查看容器详情
docker inspect codehubot-backend
```

## 调试工具推荐

### 后端工具

| 工具 | 用途 | 链接 |
|------|------|------|
| **Postman** | API 测试 | https://www.postman.com/ |
| **Apifox** | API 测试（国产） | https://www.apifox.cn/ |
| **DBeaver** | 数据库管理 | https://dbeaver.io/ |
| **Redis Insight** | Redis 管理 | https://redis.com/redis-enterprise/redis-insight/ |
| **MQTT Explorer** | MQTT 调试 | http://mqtt-explorer.com/ |

### 前端工具

| 工具 | 用途 | 扩展/链接 |
|------|------|-----------|
| **Vue DevTools** | Vue 调试 | Chrome 扩展 |
| **React DevTools** | React 调试 | Chrome 扩展 |
| **Redux DevTools** | Redux 调试 | Chrome 扩展 |
| **Vite DevTools** | Vite 分析 | 内置 |

## 调试最佳实践

### 1. 渐进式调试

```python
# ❌ 一次性写很多代码再调试
def complex_function():
    # 100 行代码...
    pass

# ✅ 分步骤调试
def complex_function():
    # 步骤1
    result1 = step1()
    print(f"步骤1结果: {result1}")  # 调试输出
    
    # 步骤2
    result2 = step2(result1)
    print(f"步骤2结果: {result2}")
    
    # 步骤3
    return step3(result2)
```

### 2. 使用断言

```python
def divide(a, b):
    assert b != 0, "除数不能为0"
    assert isinstance(a, (int, float)), "a 必须是数字"
    assert isinstance(b, (int, float)), "b 必须是数字"
    return a / b
```

### 3. 记录上下文

```python
logger.info(
    "用户登录失败",
    extra={
        "user_id": user_id,
        "ip": request.client.host,
        "user_agent": request.headers.get("User-Agent"),
        "timestamp": datetime.utcnow()
    }
)
```

### 4. 保留调试代码（但要注释）

```python
def process_data(data):
    # 调试: 打印输入数据
    # logger.debug(f"输入数据: {data}")
    
    result = expensive_operation(data)
    
    # 调试: 验证结果
    # assert result is not None, "结果不能为 None"
    
    return result
```

## 相关文档

- [本地开发环境](./本地开发环境.md) - 开发环境搭建
- [API测试](./API测试.md) - API 测试方法
- [常见问题排查](./常见问题排查.md) - 问题诊断指南
